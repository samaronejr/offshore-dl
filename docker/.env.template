# ─── Environment variables for offshore-dl Docker setup ───
# Copy to .env and fill in values

# HuggingFace token for downloading foundation model weights
HF_TOKEN=

# MLflow tracking URI (default: http://mlflow:5000 inside Docker)
MLFLOW_TRACKING_URI=http://mlflow:5000

# Optuna study storage
OPTUNA_STORAGE=sqlite:///optuna.db
